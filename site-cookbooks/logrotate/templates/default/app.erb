/var/www/a-know-home/shared/log/*.log {
    daily
    missingok
    dateext
    ifempty
    sharedscripts
    rotate 7
    su a-know a-know
    postrotate
      pid=/var/www/a-know-home/current/tmp/pids/unicorn.pid
      test -s $pid && kill -USR1 "$(cat $pid)"
    endscript
}
